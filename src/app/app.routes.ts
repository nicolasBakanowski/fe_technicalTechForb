import { Routes } from '@angular/router';

import { LoginComponent } from './components/login/login.component';

// Define tus rutas
export const routes: Routes = [
  { path: '', component: LoginComponent }, // Ruta por defecto, muestra AppComponent
];
